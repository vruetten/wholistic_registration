# 2026-08-10

## Done

- Reviewed both available code-review skills: both are diff-reviewers, unfit
  as the backbone for a whole-package review. Reusing the Fowler smell
  checklist from `mattpocock-skills:code-review` in Pass 4 only.
- Confirmed `AUDIT.md`/`REMEDIATION_PLAN.md` (May 2026) cover hygiene/
  packaging/tooling but not correctness/math/perf — the new review is
  complementary, not a redo.
- Sized the package: ~22k LOC live code; top-5 files dominate
  (`motion_correlation_pattern.py` 7,941 · `calFlowCrossResolution.py` 2,610 ·
  `main_function.py` 1,675 · `IO.py` 1,020 · `calFlow3d_Wei_v1.py` 990).
  The two biggest are also the most recently changed (2026-08-03, cyf).
- Decisions taken: journal/plan/audit committed · parallel
  fan-out + adversarial verification.
- Wrote `plan/full-package-review.md` (passes 0–4, evidence protocol,
  batch order); scaffolded `audit/`.
- Resolved leftover merge conflict on `src/.DS_Store` (untracked it — matches
  AUDIT.md F6); it was blocking all commits since PR #10.

- Researched the best public examples of package-review skills (ksimback
  tech-debt-skill, nud3l code-audit, awesome-skills code-review-skill,
  obra/superpowers reviewer) and authored a reusable project skill:
  `.claude/skills/full-package-review/` (SKILL.md + reference/ checklists and
  subagent briefs). Borrowed: churn×size orientation, mandatory "looks wrong
  but is fine" section, findings cap, RESOLVED re-runs, refute-by-default
  verification, coverage section. Slimmed `plan/full-package-review.md` to
  instance state — the skill is now the canonical process.

## Open questions

- `demos/demo_338.py` has a staged, uncommitted modification predating this
  session — commit or discard? (left untouched)

## Ran the review (same day)

- Pass 0 complete → `audit/pass-0-inventory.md` (D-001 ImmuneCell.py dead;
  motion files reclassified live-standalone; resolver bug in first graph fixed).
- Pass 1 complete → `audit/pass-1-bugs.md`. 15 reviewer agents + 20 adversarial
  verifier agents; 114 findings → **96 confirmed / 18 refuted**, 13 confirmed 🟥.
  Full evidence in `audit/pass-1-verification-log.md`. Highlights: v1 3D
  runs always use hardcoded zRatio=27.693 and re-ingest transposed warm-start
  motion; cyf's module has its GPU import misspelled (all GPU paths silently
  dead) and an artifact filter that can't fire (delta-vs-cumulative corr);
  calFlowCrossResolution unimportable on CPU + wrong-axis zRatio scaling at 2063.
- No fixes applied (review/remediation separation per skill). No cyf files touched.
- Local verification venv: numpy/scipy/matplotlib/scikit-image (+ per-verifier
  installs) in session scratchpad — rebuild as needed next session.

- Deleted the abandoned partial-rewrite subpackage (28 files) after the review
  showed its registration path never ran and nothing imported it; its 23 ledger
  findings retired (IDs stay reserved). Plan scope updated.
- Janelia-cluster skill installed at `~/.claude/skills/janelia-cluster/`;
  subagent dispatched to test ssh connectivity from this Mac and adapt it.

## Fix session (same day, after v2 deletion)

- Fixed **26 confirmed bugs**, one commit per bug (cbf274c…c33e6ce), each
  verified with a before/after repro; evidence in commit messages. Fix log +
  remaining-open triage added to `audit/pass-1-bugs.md`.
- B-001's fix was verified against real cupy 13.4.1 on the cluster (login2):
  fixed guard yields HAS_CUPY=True there, numpy fallback intact locally.
- Deferred: results-changing fixes (B-018, B-063, B-070/071, window logic),
  GPU-restructure fixes (B-034, B-067), and everything needing cyf's
  sign-off — see the ledger's "Deliberately NOT fixed" list.

## Evening session: tests + GPU + Pass 2

- Regression suite landed: tests/regressions/ — 20 passing (fixed bugs) + 5
  strict-xfail (deferred bugs); full tree 37 passed, 5 xfailed (re-verified).
- GPU job 153303848 (L4): B-001 fix verified on-device; B-019/B-089/B-090
  upgraded to CONFIRMED-run; B-034 scoped CPU-only. (Gotcha for next time:
  macOS tar ships AppleDouble ._* files — a ._*.egg-info file crashes cupy's
  install scan with UnicodeDecodeError; clean them or set COPYFILE_DISABLE=1.)
- Pass 2 complete → audit/pass-2-math.md. Verdict: numerical core SOUND
  (imresize faithful MATLAB port; warp/LK/zRatio empirically correct
  end-to-end; cyf's decomposition matches write-up, exact recovery). Live
  findings: M-C1 reference off-by-one, M-C4 getMask robustness; rest
  latent/minor. Pass-1 mask-polarity question resolved (True=outlier,
  consistent).

## Todo (next session)

- [x] Regression tests (done — 20 pass + 5 xfail)
- [x] GPU re-verification (done except getMotionUnit functional run)
- [ ] Message cyf: findings in motion_correlation_pattern.py +
      calFlowCrossResolution.py before any fixes land
- [x] Pass 2 (math) — done, audit/pass-2-math.md
- [ ] Second fix batch (results-changing: B-018, B-063, B-070/071, M-C1,
      M-C4, window logic) — after cyf/Virginia sign-off
- [ ] Pass 3 (performance) — profile on cluster where real paths run
- [ ] Pass 4 (architecture) — merge into REMEDIATION_PLAN.md Phase 4
